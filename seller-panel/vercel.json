{
  "buildCommand": "npm run build",
  "installCommand": "[ -f ../package.json ] && mv ../package.json ../package.json.tmp || true; npm install --legacy-peer-deps; [ -f ../package.json.tmp ] && mv ../package.json.tmp ../package.json || true",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
