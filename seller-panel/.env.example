# NextAuth Configuration
# URL where the seller panel is running
# For ngrok: use the seller-panel tunnel URL (tunnel: gshopseller, port 3002)
NEXTAUTH_URL=http://localhost:3002

# Secret for NextAuth session encryption
# Generate a secure random string for production: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-change-this-in-production

# Backend API URL
# Point to your GSHOP backend API
# For ngrok: use the backend tunnel URL (tunnel: gshopback, port 3000)
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1
