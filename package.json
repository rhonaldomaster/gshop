{
  "name": "gshop",
  "version": "1.0.0",
  "description": "GSHOP - TikTok Shop Clone MVP",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:admin\" \"npm run dev:seller\" \"npm run dev:creator\" \"npm run dev:mobile\"",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:admin": "cd admin-web && npm run dev",
    "dev:seller": "cd seller-panel && npm run dev",
    "dev:creator": "cd creator-panel && npm run dev",
    "dev:mobile": "cd mobile && npm start",
    "dev:adminbackend": "concurrently \"npm run dev:backend\" \"npm run dev:admin\"",
    "dev:adminseller": "concurrently \"npm run dev:backend\" \"npm run dev:seller\"",
    "build": "npm run build:backend && npm run build:admin && npm run build:seller && npm run build:creator",
    "build:backend": "cd backend && npm run build",
    "build:admin": "cd admin-web && npm run build",
    "build:seller": "cd seller-panel && npm run build",
    "build:creator": "cd creator-panel && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../admin-web && npm install && cd ../seller-panel && npm install && cd ../creator-panel && npm install && cd ../mobile && npm install",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:build": "docker compose build",
    "docker:rebuild": "docker compose down && docker compose build --no-cache && docker compose up -d",
    "docker:creator": "docker compose up creator-panel -d",
    "docker:creator:build": "docker compose build creator-panel",
    "docker:creator:logs": "docker compose logs -f creator-panel",
    "db:migrate": "cd backend && npm run migration:run",
    "db:seed": "cd backend && npm run seed",
    "test": "npm run test:backend && npm run test:admin",
    "test:backend": "cd backend && npm run test",
    "test:admin": "cd admin-web && npm run test",
    "expo-clean": "cd mobile && rm -rf .expo node_modules/.cache && npx expo start --clear --tunnel"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "backend",
    "admin-web",
    "seller-panel",
    "creator-panel",
    "mobile"
  ],
  "overrides": {
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "react-native-screens": "4.2.0",
    "nativewind": {
      "react-native-css-interop": "0.1.22"
    }
  },
  "dependencies": {
    "expo": "^54.0.30",
    "react-native-worklets": "^0.7.1"
  }
}
